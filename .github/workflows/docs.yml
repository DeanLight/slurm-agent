name: Deploy Docs

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-caching: true
      - run: uv sync --all-groups
      - run: uv run poe nb       # generate .ipynb from .py before building docs
      - run: uv run mkdocs gh-deploy --force
